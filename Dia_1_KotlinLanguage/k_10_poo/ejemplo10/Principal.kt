package k_10_poo.ejemplo10

/**
 *
 * Como pueden ver acá solamente tenemos acceso al procedimiento ´cargar´
 * y no así a las demás funciones y atributos porque se los colocó
 * con un modificador de acceso privado.
 *
 * */

fun main() {
    val operaciones1 = Operaciones()
    operaciones1.cargar()
}